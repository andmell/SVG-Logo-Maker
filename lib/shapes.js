class Shapes {
    constructor (x, y, width, height, stroke, fill, sWidth,) {
        this.x = x,
        this.y = y,
        this.width = width,
        this.height = height,
        this.stroke = stroke,
        this.fill = fill,
        this.sWidth = sWidth;
    }
};

class Circle extends Shapes {
    constructor(cx, cy, r) {
        super(stroke, fill, sWidth)
        this.cx = reponse.cx,
        this.cy = response.cy;

    }
};

class Square extends Shapes {
    constructor(){
        super(x, y, width, height, stroke, fill, sWidth)
    }
}